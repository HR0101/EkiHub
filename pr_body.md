## 概要

操作の流れ（最寄駅 → 算出 → 調整）を分かりやすくするための「手順ガイド機能」と、カードやボタンに「ネオモーフィズムの押し込み（プレス）演出」を追加しました。

## 変更内容

- `public/js/features/stepGuide.js` の追加: 現在のステップを自動判定し、該当UIを発光リングで強調・スクロールするロジック
- `public/index.html` の更新: 手順ガイドの描画先となる拡張枠 `#feature-steps` を追加し、スクリプトを読み込み
- `public/style.css` の更新: ボタン等の `:active` 時に沈み込むようなプレス演出を一括管理するためのCSS変数を追加（アクセシビリティ考慮の `@media (prefers-reduced-motion: reduce)` も対応）

## 動作確認

- [x] `node scripts/testRouting.js` が ALL PASS になる
- [x] `npm start` で起動し，トップページの中心駅算出が動作する
- [x] 変更したページで表示崩れ・コンソールエラーがない

<details>
<summary>UI を変更した場合</summary>

- [x] ライトモードとダークモードの両方で確認した
- [x] カラーテーマ（サクラ／森／海／夕焼け／紅葉）で色が破綻していない
- [x] 高コントラストテーマで文字が読める
- [x] 文字と背景のコントラストが基準を満たす（本文 4.5:1／大きな文字 3:1）

</details>

<details>
<summary>機能を追加した場合</summary>

- [x] `public/js/features/` の独立モジュールとして追加した（コアの `app.js` を編集していない）
- [x] モジュールが失敗しても他の機能に影響しない（例外を握っている）
- [ ] 外部 API を呼ぶ場合，キャッシュ・タイムアウト・失敗時フォールバックを実装した （※今回は利用なし）

</details>

## スクリーンショット

UIの追加・変更を含むため、各ステップの強調リングや、ボタン押下時の沈み込みアニメーションが確認できます。

## デプロイへの影響

- [x] `main` へマージすると本番（https://ekihub.jp ）へ自動デプロイされることを理解している
- [x] 環境変数・GitHub Secrets の追加や変更はない

## 関連 Issue

- 特になし
